server:
  port: 8080
spring:
  cache:
    redis:
      time-to-live: 1800000
  application:
    name: example
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/oranges?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: root
      password: wn204420.
  main:
    banner-mode: off
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0
      password: wn204420.
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID
    banner: false
Utils:
  accessKeyId:
  accessKeySecret:
logging:
  level:
    web: WARN

